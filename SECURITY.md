# 安全说明

## API key

不要把真实高德 API key 提交到仓库。推荐两种方式：

- 页面运行时输入 key。
- 使用环境变量 `AMAP_KEY` 启动本地服务。

`.env` 和 `work/` 默认被 `.gitignore` 忽略。

## 上线提醒

如果要公开部署，建议：

- 使用域名限制后的高德 JS key。
- 后端保存 Web 服务 key，前端不要直接暴露。
- 给路线接口增加限流和请求配额保护。
- 避免记录用户精确起点到长期日志中。

## 报告问题

如果发现 key 泄露、错误路线缓存或数据误匹配，请先暂停公开部署，清理相关缓存和提交历史，再更新 key。
